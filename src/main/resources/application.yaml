spring:
  application:
    name: eyes
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres}
    username: ${DB_USERNAME:postgres.gycrgtbpimcpctsxdqyr}
    password: ${DB_PASSWORD}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none

server:
  port: ${PORT:8080}
